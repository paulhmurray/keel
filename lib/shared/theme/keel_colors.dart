import 'package:flutter/material.dart';

abstract class KColors {
  static const bg        = Color(0xFF0a0d0f);
  static const surface   = Color(0xFF0f1418);
  static const surface2  = Color(0xFF141a20);
  static const border    = Color(0xFF1e2830);
  static const border2   = Color(0xFF253040);
  static const amber     = Color(0xFFe8a430);
  static const amberDim  = Color(0xFF3d2a0a);
  static const phosphor  = Color(0xFF2dd4a0);
  static const phosDim   = Color(0xFF0a2e24);
  static const red       = Color(0xFFe05252);
  static const redDim    = Color(0xFF2e1010);
  static const blue      = Color(0xFF4a9eff);
  static const blueDim   = Color(0xFF0d1f3a);
  static const text      = Color(0xFFc8d4dc);
  static const textDim   = Color(0xFF5a7080);
  static const textMuted = Color(0xFF2a3840);
}
